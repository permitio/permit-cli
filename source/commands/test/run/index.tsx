import React from 'react';
import { Text } from 'ink';

export default function Index() {
	return (
		<Text>
			Use one of the subcommands to run tests: - audit: Test PDP against
			historical audit logs
		</Text>
	);
}
